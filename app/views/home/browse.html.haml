%h3
  Featured Recipes
%section.featured
  - @images.each do |image, recipe|
    %div.rec-item
      %a.recipe-link{:href => recipe_path(recipe)}
        %img{:src => image}
        .item-text
          = recipe.title
      %ul.recipe-stats
        %li.forks
          %span.label
            = pluralize(recipe.number_of_forks, 'fork')
        %li.saves
          %span.label
            = pluralize(recipe.number_of_stars, 'save')

